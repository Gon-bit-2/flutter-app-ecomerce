import 'package:flutter/material.dart';

class AppColors {
  // Primary Blue
  static const Color primaryBlue = Color(
    0xFF1877F2,
  ); // Match the blue in design
  static const Color darkBlue = Color(0xFF0D47A1);

  // Background
  static const Color background = Colors.white;
  static const Color inputBackground = Color(
    0xFFF5F6FA,
  ); // Light grey for inputs

  // Text
  static const Color textPrimary = Color(0xFF1F2937); // Dark grey/black
  static const Color textSecondary = Color(0xFF6B7280); // Grey text
  static const Color textLight = Colors.white;

  // Borders
  static const Color border = Color(0xFFE5E7EB);

  // Status
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF10B961);
}
